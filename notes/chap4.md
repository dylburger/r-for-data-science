Use `Cmd + Shift + N` to open the script editor. *Keep code you care about here*.

When in the script editor, `Cmd + Enter` will execute the current line of code your cursor is on.

`Cmd + Shift + S` executes the whole script.
